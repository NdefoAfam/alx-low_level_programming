#include "main.h"
/**
 * _isdigit - function that varies if a character is a digit or not
 * @c: tested character
 * Return: returs 1 if it is upper case, 0 if not
 */

int _isdigit.c(int c)
{
if ((c >= 48 && (c <= 57))
return (1);

return(0);
}

