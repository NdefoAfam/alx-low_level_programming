#include "main.h"
/**
* _isupper - checks if parameter is uppercase
* @c: input character
* Return: 1 if is an uppercase or 0 otherwise
*/

int _isupper(int c)

char c
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
