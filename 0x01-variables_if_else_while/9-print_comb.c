#include <stdio.h>

/**
 * main - prints all combinations of single-digit
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num;

	for (n = 48; n < 58; n++)
	{
		putchar(num);
		if (num != 57)
		{
			putchar(',');
			putchar(' ');
		}

		putchar('\n');
		return (0);
	}
